{
  "task_id": "test-logging-feature-20260130",
  "title": "Test Logging Feature",
  "description": "Test the new logging feature to verify stdout/stderr capture and duration tracking.",
  "status": "pending",
  "priority": "high",
  "created_at": "2026-01-30T01:45:00Z",
  "context": {
    "purpose": "Verify that the task monitor system properly captures stdout, stderr, and duration",
    "expected_outputs": [
      "stdout field in result JSON with printed text",
      "stderr field if any errors are printed",
      "duration_seconds > 0",
      "systemd journal entries with [task_id] prefix"
    ]
  },
  "requirements": [
    "Print a test message to stdout",
    "Print a test message to stderr",
    "Complete successfully",
    "Return structured output confirming the test"
  ],
  "success_criteria": [
    "Result JSON contains stdout field",
    "Result JSON contains stderr field",
    "Result JSON contains duration_seconds > 0",
    "Task status is 'completed'"
  ],
  "worker_instructions": {
    "investigation": "This is a simple test of the logging system. You don't need to investigate anything complex.",
    "implementation": [
      "Use Python to print test messages",
      "Print to stdout using print()",
      "Print to stderr using sys.stderr.write()",
      "Create a simple delay (e.g., time.sleep(0.5)) to ensure duration > 0",
      "Return a success result"
    ],
    "verification": [
      "Confirm stdout was printed",
      "Confirm stderr was printed",
      "Return a completion message"
    ],
    "output_format": {
      "type": "test_result",
      "fields": ["stdout_test", "stderr_test", "completion_status"]
    }
  }
}

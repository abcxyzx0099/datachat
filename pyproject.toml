[project]
name = "task-monitor-cli"
version = "1.0.0"
description = "Task Monitor CLI"

[project.scripts]
task-monitor = "cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["cli"]
